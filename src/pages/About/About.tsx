import React from 'react';
import styled from 'styled-components';

export type AboutProps = {
}

const About: React.FC<AboutProps>  = ({}) => {
	return <AboutStl>About</AboutStl>;
};

export const AboutStl = styled.div``;

export default About;
