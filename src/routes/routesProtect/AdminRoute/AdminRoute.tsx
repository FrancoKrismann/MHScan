import React from 'react';
import styled from 'styled-components';

export type AdminRouteProps = {
}

// const AdminRoute = ({ element: Element, ...rest }) => {
// 	return (
// 	  <Route
// 		{...rest}
// 		element={isAuthenticated() && isAdmin() ? (
// 		  <Element />
// 		) : (
// 		  <Navigate to="/login" />
// 		)}
// 	  />
// 	);
//   };

export const AdminRouteStl = styled.div``;

// export default AdminRoute;
